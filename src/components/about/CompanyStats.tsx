import React from 'react';
import { StatsCounter } from '@/components/home/StatsCounter';

// Reusing the StatsCounter component from home page
// Can be customized with different stats if needed

export const CompanyStats: React.FC = () => {
  return <StatsCounter />;
};
